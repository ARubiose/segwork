"""The segwork.models.backbone package provides reusable backbones

Backbone could be a single encoder, a multi-stream encoder (e.g. RGB-D)

Backbones repositories:
 * Torchvision
 * timm models

"""

from .base import *